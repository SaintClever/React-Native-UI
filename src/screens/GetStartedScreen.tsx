import { View, Text, StyleSheet, Image, ImageBackground } from "react-native";

const GetStartedScreen = () => {
  return(
    <View><Text>GetStartedScreen</Text></View>
  );
};

export default GetStartedScreen;

const styles = StyleSheet.create({});